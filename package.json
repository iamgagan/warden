{
  "name": "warden",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": { "node": ">=22" },
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "pnpm -r build",
    "demo": "pnpm build && node scripts/demo.mjs",
    "test": "vitest run",
    "typecheck": "pnpm -r build"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "stripe": "^17.4.0",
    "typescript": "^5.5.4",
    "vitest": "^3.0.0"
  }
}
